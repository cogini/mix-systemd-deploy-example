#!/usr/bin/env bash

# Get remote console on app
# This needs to be run under the app user account

# Exit on errors
set -e
# set -o errexit -o xtrace

# CURDIR="$PWD"
# BINDIR=$(dirname "$0")
# cd "$BINDIR"; BINDIR="$PWD"; cd "$CURDIR"

# BASEDIR="$BINDIR/.."
# cd "$BASEDIR/current"

# Config vars
DEPLOY_DIR="/srv/mix-systemd-deploy"
APP_NAME="mix_systemd_deploy"
export LANG="en_US.UTF-8"
export RELEASE_MUTABLE_DIR="/run/mix-systemd-deploy"
CURRENT_DIR="$DEPLOY_DIR/current"

$CURRENT_DIR/bin/$APP_NAME remote_console
